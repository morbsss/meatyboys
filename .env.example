# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# ── LOCAL DEVELOPMENT (these defaults work out of the box) ───────────────────
SECRET_KEY=dev-secret-key-change-in-production
DEBUG=true
HOST=127.0.0.1
PORT=3000

# ── PRODUCTION (VM) — uncomment and fill in ──────────────────────────────────
# Generate a secret key first:
  python -c "import secrets; print(secrets.token_hex(32))"

SECRET_KEY="g*K2e=fmsM%-+zw("
DEBUG=false
HOST=149.28.178.14
PORT=5000           ← open port on your VM (e.g. 8080, 5000)
#
# Only needed if you move the data folder:
# DATA_DIR=/home/ubuntu/meatyboys/data
# PLAYERFILES_DIR=/home/ubuntu/meatyboys/playerfiles
